# Debian bookworm with XFCE
Vagrantfile in this folder provides a Debian bookworm virtual machine with XFCE desktop environment. 


To create the system and run it use the command:
```
vagrant up
```

To connect to the system use the command:
```
vagrant ssh
```

